# CLAUDE.md — Development Context

This file provides context for AI-assisted development (Claude Code, Copilot, etc.).

## Environment

```bash
# Run from projects/all-weather/
conda run -n allweather python3 <script>
conda run -n allweather python3 -m pytest tests/ -v
```

## Module Map

### Core Engine

| File | Purpose |
|---|---|
| `config.py` | All parameters. Loads allocation from `strategies.json` |
| `main.py` | Entry point for single backtest runs |
| `backtest.py` | Simulation engine: `run_backtest`, `run_backtest_rolling_rp`, `compute_stats` |
| `optimiser.py` | `compute_risk_parity_weights()` (SLSQP), random/calmar search |
| `data.py` | yfinance price fetching with data quality checks |
| `export.py` | Results folders, Excel master log, terminal formatting |
| `plotting.py` | Dark-theme matplotlib charts |
| `portfolio.py` | Live portfolio state and rebalancing instructions |
| `validation.py` | Walk-forward and Pareto frontier analysis |

### Active Scripts

| File | Purpose |
|---|---|
| `compare_allw.py` | ALLW ETF head-to-head comparison (charts + Excel + metrics) |
| `plot_linkedin.py` | Two-panel LinkedIn figure (equity curves + drawdown zoom) |
| `alpaca_monthly_rebalance.py` | Paper trading via Alpaca (multi-account, preview + execute) |
| `run_rolling_rp.py` | Rolling RP vs static RP experiment (3-split validation) |

### Configuration

| File | Purpose |
|---|---|
| `strategies.json` | Strategy registry: allocations, live tickers, OOS results |
| `portfolio_holdings.json` | Current Alpaca holdings (updated by rebalance script) |

## Key Constraints

- **IS/OOS discipline:** Never optimise on data after OOS_START. RP covariance uses `end_date` parameter to enforce this.
- **Calmar ratio** is the primary evaluation metric (CAGR / |max drawdown|).
- **Risk parity** does not optimise for returns — it equalises risk contributions via covariance matrix only.
- Production weights (6asset_tip_gsg_rpavg): SPY 13.4%, QQQ 10.3%, TLT 17.5%, TIP 34.8%, GLD 14.2%, GSG 9.8%.
- `results/` is .gitignore'd — all output is regenerated by running scripts.

## Closed Investigations

| Investigation | Experiments | Result |
|---|---|---|
| Differential Evolution | 26 | All fail OOS — structural regime mismatch |
| SPY momentum overlay | 126 combos | Does not add value — re-entry timing unsolvable |
| Rolling RP | 3 splits | Converges to same weights as static |
| Weekly rebalancing | 3 splits | No improvement after transaction costs |
| 100-ETF universe scan | 50k subsets | 6-asset universe confirmed optimal |
| 8-asset universe | 3 splits | 6-asset wins on all Calmar windows |
| Bond leverage (1.0x–2.5x) | 7 levels × 3 splits | Destroys Calmar in rising-rate regime |

## Archive

Completed experiment scripts live in `archive/`:
- `run_8asset_experiments.py` — 8-asset validation (6-asset proven superior)
- `run_leverage_experiment.py` — bond leverage sweep (destroys Calmar)
- `scan_universes.py` — 100-ETF random sampling scanner (confirmed 6-asset)
- `run_rp_validation.py` — 3-split RP vs manual (RP wins all 3, results in strategies.json)
- `run_experiment.py` — batch DE pipeline (DE closed)
- `results_dashboard.py` — old dashboard generator
- `optimiser_de.py` — archived DE code
