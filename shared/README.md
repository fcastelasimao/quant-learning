# Shared Utilities

Shared modules used across multiple projects.

Currently empty. `regime.py` in `projects/wave-rider/` is a candidate for extraction here once it stabilises and is needed by more than one project.
